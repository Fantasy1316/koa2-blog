/**
 * @description Sequelize 数据类型
 * @author Alen
 */

const Sequelize = require('sequelize')

module.exports = {
  STRING: Sequelize.STRING,
  DECIMAL: Sequelize.DECIMAL,
  TEXT: Sequelize.TEXT,
  INTEGAR: Sequelize.INTEGER,
  BOOLEAN: Sequelize.BOOLEAN
}